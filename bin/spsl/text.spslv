#VS_IN vec3 pos;
#VS_IN vec2 uv;

#VS_OUT vec2 tex_coords;

#UNIFORM mat4 uProj;
#UNIFORM mat4 uView;
#UNIFORM mat4 uModel;

void main() {
    spsl_position = mul(uProj, mul(uView, mul(uModel, vec4(pos, 1.0))));
    tex_coords = uv;
}
