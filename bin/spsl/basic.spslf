#FS_IN vec3 color_out;

#UNIFORM vec3 uColorMult;
#UNIFORM float uTime;

void main() {
    vec3 color = color_out*uColorMult;
    color.g = sin(uTime)*0.5 + 0.5;
    spsl_fragcolor = vec4(color, 1.0);
}